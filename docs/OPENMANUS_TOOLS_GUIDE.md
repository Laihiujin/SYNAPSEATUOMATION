# OpenManus 工具集成指南

> 本文档介绍 SynapseAutomation 系统中集成的 OpenManus AI Agent 工具集合

## 📋 目录

- [工具概览](#工具概览)
- [基础工具](#基础工具)
- [账号管理](#账号管理)
- [素材管理](#素材管理)
- [发布管理](#发布管理)
- [任务管理](#任务管理)
- [矩阵发布](#矩阵发布)
- [数据采集](#数据采集)
- [平台操作](#平台操作)
- [数据分析](#数据分析)
- [系统管理](#系统管理)

---

## 工具概览

OpenManus Agent 现已集成 **30+** 个专业工具，覆盖社交媒体自动化的全流程：

### 工具分类统计

| 类别 | 工具数量 | 主要功能 |
|------|---------|---------|
| 基础工具 | 1 | 通用 API 调用 |
| 账号管理 | 1 | 账号列表、状态查询 |
| 素材管理 | 3 | 文件列表、详情、搜索 |
| 发布管理 | 4 | 批量发布、预设管理 |
| 任务管理 | 2 | 任务状态、列表查询 |
| 矩阵发布 | 4 | 多平台批量发布 |
| 数据采集 | 2 | MediaCrawler、视频号采集 |
| 平台操作 | 2 | 账号登录、状态检查 |
| 数据分析 | 1 | 统计报告、趋势分析 |
| 系统管理 | 4 | Cookie、IP池、脚本执行 |

---

## 基础工具

### 1. call_api - 通用 API 调用工具

**描述**: 调用 SynapseAutomation 后端的任何 API 接口

**使用场景**:
- 调用未封装的 API 端点
- 执行自定义操作
- 系统级配置修改

**参数**:
```json
{
  "endpoint": "/accounts",           // API 端点路径
  "method": "GET",                   // HTTP 方法
  "params": {},                      // 查询参数
  "json_data": {}                    // 请求体
}
```

**示例**:
```python
# 获取所有账号
await call_api(endpoint="/accounts", method="GET")

# 创建新任务
await call_api(
    endpoint="/tasks",
    method="POST",
    json_data={"task_type": "publish", "priority": 5}
)
```

---

## 账号管理

### 2. list_accounts - 列出账号

**描述**: 获取系统中所有可用的社交媒体账号列表

**参数**:
```json
{
  "platform": "douyin",              // 平台筛选（可选）
  "status": "active"                 // 状态筛选: active/inactive/all
}
```

**返回示例**:
```
📋 找到 15 个账号：
- [ID: acc001] douyin - 张三的账号 (valid)
- [ID: acc002] xiaohongshu - 李四工作室 (valid)
- [ID: acc003] kuaishou - 王五主号 (expired)
```

**支持的平台**:
- `douyin` - 抖音
- `kuaishou` - 快手
- `xiaohongshu` - 小红书
- `bilibili` - B站
- `channels` - 微信视频号

---

## 素材管理

### 3. list_assets / list_files - 列出素材

**描述**: 获取素材库中的视频/图片列表

**参数**:
```json
{
  "file_type": "video",              // 文件类型: video/image/audio/all
  "limit": 20,                       // 返回数量
  "keyword": "旅游"                  // 搜索关键词（可选）
}
```

### 4. get_file_detail - 获取文件详情

**描述**: 获取指定文件的详细信息

**参数**:
```json
{
  "file_id": 123                     // 文件ID
}
```

**返回示例**:
```
📄 文件详情：
- ID: 123
- 文件名: 旅游vlog.mp4
- 类型: video
- 大小: 25.43 MB
- 时长: 180秒
- 状态: pending
```

---

## 发布管理

### 5. publish_batch_videos - 批量发布视频

**描述**: 批量发布视频到多个平台和账号，支持差异化配置

**关键规则**:
1. 话题标签（topics）必须恰好 **4 个**
2. 多平台发布建议使用 `items` 参数指定差异化内容
3. 平台代码: 1=小红书, 2=视频号, 3=抖音, 4=快手, 5=B站

**参数**:
```json
{
  "file_ids": [1, 2],                // 视频文件ID列表
  "accounts": ["acc1", "acc2"],      // 目标账号ID列表
  "platform": 3,                     // 平台代码（可选）
  "title": "统一标题",               // 统一标题
  "description": "统一描述",         // 统一描述
  "topics": ["tag1", "tag2", "tag3", "tag4"],  // 必须4个标签
  "items": [                         // 差异化配置（可选）
    {
      "file_id": 1,
      "title": "抖音专属标题",
      "topics": ["抖音tag1", "tag2", "tag3", "tag4"]
    }
  ],
  "priority": 5                      // 任务优先级 1-10
}
```

**示例**:
```python
# 单平台单视频发布
await publish_batch_videos(
    file_ids=[123],
    accounts=["acc001"],
    title="我的第一条视频",
    topics=["旅游", "vlog", "风景", "推荐"]
)

# 多平台差异化发布
await publish_batch_videos(
    file_ids=[123],
    accounts=["dy_acc", "xhs_acc"],
    items=[
        {
            "file_id": 123,
            "title": "抖音版标题 #旅游vlog",
            "topics": ["旅游", "抖音", "风景", "推荐"]
        },
        {
            "file_id": 123,
            "title": "小红书｜旅行日记📷",
            "topics": ["旅游", "小红书", "风景", "种草"]
        }
    ]
)
```

### 6. create_publish_preset - 创建发布预设

**描述**: 创建发布配置模板，用于快速批量发布

**参数**:
```json
{
  "name": "日常发布预设",
  "platform": 3,                     // 平台代码
  "accounts": ["acc1", "acc2"],
  "default_title_template": "【日常】{title}",
  "default_topics": ["日常", "生活", "vlog", "分享"]
}
```

### 7. list_publish_presets - 列出发布预设

### 8. use_preset_to_publish - 使用预设发布

**参数**:
```json
{
  "preset_id": 1,
  "file_ids": [123, 456],
  "override_title": "自定义标题",    // 可选覆盖
  "override_accounts": ["acc3"]      // 可选覆盖
}
```

---

## 任务管理

### 9. get_task_status - 获取任务状态

**参数**:
```json
{
  "task_id": "task_123"
}
```

### 10. list_tasks_status - 列出任务状态

**参数**:
```json
{
  "status": "running",               // pending/running/success/error/all
  "limit": 20
}
```

---

## 矩阵发布

矩阵发布支持多平台、多账号、多素材的组合发布，实现最大化覆盖。

### 11. generate_matrix_tasks - 生成矩阵任务

**描述**: 生成多平台矩阵发布任务

**参数**:
```json
{
  "platforms": ["douyin", "xiaohongshu", "kuaishou"],
  "material_ids": ["123", "456"],
  "title": "统一标题",
  "topics": ["tag1", "tag2", "tag3", "tag4"],
  "batch_name": "2025春节矩阵发布"
}
```

**工作流程**:
1. 自动获取各平台所有有效账号
2. 为每个"平台-账号-素材"组合生成任务
3. 任务进入队列等待执行

**返回示例**:
```
✅ 矩阵任务已生成成功！

**批次信息**：
- 批次 ID: batch_20250307_001
- 批次名称: 2025春节矩阵发布

**任务统计**：
- 总任务数: 45
- 平台数: 3
- 账号数: 15
- 素材数: 2

**平台分布**：
  - douyin: 8 个账号
  - xiaohongshu: 5 个账号
  - kuaishou: 2 个账号
```

### 12. get_matrix_status - 获取矩阵状态

**描述**: 查看矩阵任务队列的执行状态

### 13. execute_matrix_task - 执行单个矩阵任务

**描述**: 从队列中弹出并执行下一个任务

### 14. execute_all_matrix_tasks - 批量执行矩阵任务

**描述**: 循环执行所有待处理任务

**参数**:
```json
{
  "max_tasks": 100                   // 最大执行数量（防止无限循环）
}
```

---

## 数据采集

### 15. media_crawler - MediaCrawler 数据采集

**描述**: 使用 MediaCrawler 采集社交媒体数据

**支持平台**:
- 抖音（视频、评论、用户）
- 小红书（笔记、评论、用户）
- 快手（视频、评论、用户）
- B站（视频、评论、用户）

**参数**:
```json
{
  "platform": "douyin",
  "keywords": ["旅游", "美食", "vlog"],
  "max_count": 20,                   // 每个关键词最大采集数量
  "crawl_type": "search"             // search/user/video/note
}
```

**示例**:
```python
# 采集抖音"旅游"相关视频
await media_crawler(
    platform="douyin",
    keywords=["旅游"],
    max_count=50,
    crawl_type="search"
)

# 采集小红书用户笔记
await media_crawler(
    platform="xiaohongshu",
    keywords=["user:12345"],
    max_count=100,
    crawl_type="user"
)
```

### 16. wechat_channels_crawler - 视频号采集

**描述**: 采集微信视频号数据

**参数**:
```json
{
  "keyword": "旅游",                 // 搜索关键词
  "user_id": "xxx",                  // 用户ID（可选）
  "max_count": 20,
  "sort_type": "最新"                // 最新/最热
}
```

---

## 平台操作

### 17. platform_login - 平台账号登录

**描述**: 执行平台账号登录操作

**参数**:
```json
{
  "platform": "douyin",              // 平台名称
  "account_id": "acc001",            // 账号ID（可选，新账号留空）
  "login_method": "qrcode"           // qrcode/sms/password
}
```

**返回示例**:
```
✅ 登录会话已创建！
- 平台: douyin
- 会话 ID: session_abc123
- 状态: pending
- 二维码地址: http://localhost:7000/qr/session_abc123.png

请使用手机 App 扫描二维码完成登录。
```

### 18. check_login_status - 检查登录状态

**描述**: 检查扫码登录是否完成

**参数**:
```json
{
  "platform": "douyin",
  "session_id": "session_abc123"
}
```

**返回示例**:
```
📊 登录状态检查结果：
- 平台: douyin
- 状态: success
- 账号 ID: dy_user_001
- 账号名: 张三的抖音
✅ 登录成功！
```

---

## 数据分析

### 19. data_analytics - 数据分析报告

**描述**: 获取数据分析报告和统计信息

**报告类型**:
- `publish_stats` - 发布统计
- `engagement` - 互动数据
- `growth` - 增长趋势
- `trends` - 热门趋势

**参数**:
```json
{
  "report_type": "publish_stats",
  "platform": "douyin",              // 平台筛选（可选）
  "start_date": "2025-01-01",
  "end_date": "2025-03-07"
}
```

**示例**:
```python
# 获取抖音本月发布统计
await data_analytics(
    report_type="publish_stats",
    platform="douyin",
    start_date="2025-03-01",
    end_date="2025-03-07"
)

# 获取所有平台增长趋势
await data_analytics(
    report_type="growth",
    start_date="2025-01-01"
)
```

---

## 系统管理

### 20. get_system_context - 获取系统上下文

**描述**: 获取完整的系统状态信息（账号、素材、发布记录等）

### 21. get_dashboard_stats - Dashboard 统计

**描述**: 获取系统整体统计数据

**返回示例**:
```
📊 系统统计数据：

**账号统计**：
- 总账号数: 25
- 有效账号: 22

**素材统计**：
- 总素材数: 150
- 待发布: 80

**发布统计**：
- 总发布数: 500
- 今日发布: 15
- 待处理任务: 3
```

### 22. ip_pool_manager - IP 池管理

**描述**: 管理代理 IP 池

**操作类型**:
- `list` - 列出所有 IP
- `add` - 添加新 IP
- `remove` - 删除 IP
- `test` - 测试 IP 可用性

**参数**:
```json
{
  "action": "add",
  "ip_address": "192.168.1.100",
  "port": 8080,
  "username": "proxy_user",          // 可选
  "password": "proxy_pass"           // 可选
}
```

### 23. cookie_manager - Cookie 管理

**描述**: 管理账号 Cookie

**操作类型**:
- `list` - 列出 Cookie
- `export` - 导出 Cookie
- `import` - 导入 Cookie
- `refresh` - 刷新 Cookie

**参数**:
```json
{
  "action": "export",
  "account_id": "acc001"
}
```

### 24. run_backend_script - 运行后端脚本

**描述**: 执行后端预定义的 Python 脚本

**参数**:
```json
{
  "script_name": "export_data",      // 脚本名称（不含.py）
  "args": {                          // 脚本参数
    "platform": "douyin",
    "start_date": "2025-01-01"
  }
}
```

### 25. execute_python_script - 执行 Python 代码

**描述**: 直接执行 Python 代码片段（⚠️ 谨慎使用）

**参数**:
```json
{
  "code": "print('Hello, World!')",
  "description": "测试脚本"
}
```

---

## 使用建议

### 1. 工作流推荐

**内容发布流程**:
```
1. list_accounts → 查看可用账号
2. list_files → 选择要发布的素材
3. publish_batch_videos → 执行发布
4. get_task_status → 监控发布状态
```

**矩阵发布流程**:
```
1. get_system_context → 了解系统状态
2. generate_matrix_tasks → 生成矩阵任务
3. get_matrix_status → 查看队列状态
4. execute_all_matrix_tasks → 批量执行
```

**数据采集流程**:
```
1. media_crawler → 启动采集任务
2. get_task_status → 监控采集进度
3. list_files → 查看采集到的数据
```

### 2. 最佳实践

- **话题标签**: 发布时务必提供恰好 4 个话题标签
- **差异化内容**: 多平台发布时使用 `items` 参数定制内容
- **批量操作**: 优先使用批量工具（batch、matrix）提高效率
- **状态监控**: 定期使用状态查询工具监控任务进度
- **错误处理**: 任务失败时查看 `error_message` 字段

### 3. 安全注意事项

- Cookie 和 IP 信息敏感，导出后妥善保管
- 执行 Python 脚本前确认代码安全性
- 避免高频 API 调用触发平台限流
- 定期备份账号配置和 Cookie

---

## 工具调用示例

### 场景 1: 新账号登录并发布

```python
# 1. 登录新账号
result = await platform_login(
    platform="douyin",
    login_method="qrcode"
)
session_id = result["session_id"]

# 2. 等待用户扫码，检查登录状态
status = await check_login_status(
    platform="douyin",
    session_id=session_id
)

# 3. 登录成功后查看账号列表
accounts = await list_accounts(platform="douyin")

# 4. 选择素材并发布
await publish_batch_videos(
    file_ids=[123],
    accounts=[accounts[0]["account_id"]],
    title="我的第一条抖音视频",
    topics=["新人", "首发", "vlog", "日常"]
)
```

### 场景 2: 多平台内容分发

```python
# 1. 查看可用账号
dy_accounts = await list_accounts(platform="douyin", status="active")
xhs_accounts = await list_accounts(platform="xiaohongshu", status="active")

# 2. 准备差异化内容
items = [
    {
        "file_id": 123,
        "title": "抖音版｜春日旅行vlog🌸",
        "topics": ["旅游", "春天", "vlog", "推荐"]
    },
    {
        "file_id": 123,
        "title": "小红书｜春日旅行攻略📷",
        "topics": ["旅游攻略", "春天", "种草", "出游"]
    }
]

# 3. 批量发布
await publish_batch_videos(
    file_ids=[123],
    accounts=[dy_accounts[0]["account_id"], xhs_accounts[0]["account_id"]],
    items=items
)
```

### 场景 3: 数据采集分析

```python
# 1. 采集竞品数据
await media_crawler(
    platform="douyin",
    keywords=["旅游vlog", "美食探店"],
    max_count=100,
    crawl_type="search"
)

# 2. 等待采集完成，查看数据
files = await list_files(keyword="旅游vlog", limit=50)

# 3. 分析自己的发布数据
report = await data_analytics(
    report_type="engagement",
    platform="douyin",
    start_date="2025-03-01"
)
```

---

## 常见问题

### Q1: 为什么话题标签必须是 4 个?

A: 这是后端 API 的验证规则，确保内容符合平台推荐算法要求。不足或超过 4 个会返回错误。

### Q2: 如何实现定时发布?

A: 使用 `publish_batch_videos` 的 `scheduled_time` 参数（需要后端支持）。

### Q3: 矩阵任务如何避免重复发布?

A: 系统会自动分配，确保每个"账号-素材"组合只发布一次。

### Q4: Cookie 过期后如何处理?

A: 使用 `cookie_manager(action="refresh")` 刷新，或重新登录。

### Q5: 如何查看工具调用日志?

A: 检查后端日志文件 `syn_backend/logs/` 目录。

---

## 版本历史

- **v1.0.0** (2025-03-07)
  - 初始版本，集成 25 个基础工具
  - 支持抖音、快手、小红书、B站、视频号

- **v1.1.0** (2025-03-07)
  - 新增 8 个扩展工具
  - 新增数据采集、IP池管理、Cookie管理
  - 优化工具描述和参数验证
  - 总工具数达到 33 个

---

## 技术支持

- 文档更新日期: 2025-03-07
- 适用版本: OpenManus v0.1.0+
- 后端 API 版本: v1
- 问题反馈: 提交 GitHub Issue

---

**Happy Automating! 🚀**
