# 🚀 SynapseAutomation 快速启动指南

## ⚡ 一键启动(推荐)

```bash
start_electron_simple.bat
```

**这个脚本会:**
- ✅ 所有服务在**后台静默运行**(不会弹出多个窗口)
- ✅ 只有一个Electron窗口
- ✅ 无闪烁,无timeout延迟
- ✅ 启动速度快(8秒等待)

---

## 🛑 停止所有服务

```bash
stop_quick.bat
```

一键停止所有后台服务(Redis, Celery, Playwright, FastAPI, React, Electron)

---

## 🎯 启动后打开的内容

Electron窗口会自动显示:
- **默认首页**: `http://127.0.0.1:3000` (你的前端UI)
- **URL导航栏**: 可以输入任意网址访问
- **左侧标签栏**: 类似Flowith的多标签管理

---

## 📝 对比旧版启动脚本

| 脚本 | 窗口数量 | 闪烁 | 推荐 |
|------|---------|------|------|
| `start_electron_app.bat` | 6个窗口 | ❌ 严重 | ❌ 不推荐 |
| `start_all_services_synenv.bat` | 5个窗口 | ❌ 中等 | ❌ 不推荐 |
| **`start_electron_simple.bat`** | **1个窗口** | **✅ 无** | **✅ 强烈推荐** |

---

## 🔧 故障排除

### 如果启动失败

1. **检查端口占用**:
   ```bash
   netstat -ano | findstr ":7000 :7001 :3000 :6379"
   ```

2. **手动停止旧服务**:
   ```bash
   stop_quick.bat
   ```

3. **重新启动**:
   ```bash
   start_electron_simple.bat
   ```

### 如果想查看服务日志

后台服务的输出被重定向到了 `nul`,如果需要调试,可以临时注释掉启动脚本中的 `>nul 2>&1`

---

## 📦 打包

```bash
cd desktop
build.bat
```

输出: `desktop\dist-installer\SynapseAutomation-Setup-1.0.exe`

---

**享受你的Flowith风格浏览器!** 🎉
